{
  "name": "wormular-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute wormular --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute wormular --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250913.0",
    "typescript": "~5.9.3",
    "wrangler": "^4.34.0"
  }
}
