{
  "name": "wormular-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "db:init": "npx wrangler d1 execute wormular --local --file=./schema.sql",
    "db:init:remote": "npx wrangler d1 execute wormular --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250913.0",
    "typescript": "~5.9.3",
    "wrangler": "^4.34.0"
  },
  "dependencies": {
    "obscenity": "^0.4.6"
  }
}
