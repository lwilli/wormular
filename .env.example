VITE_API_URL=
# Production (GitHub Actions): repository secret VITE_API_URL = https://wormular-api.lwilli.workers.dev
# Local .env: same URL to hit prod API from Vite, or leave empty and run `npm run dev:all` (proxies /api).
